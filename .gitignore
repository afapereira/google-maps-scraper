cache/
vendor
bin
webdata/
examples/examples-api/map-outputs
examples/examples-api/**/__pycache__/
examples/examples-api/**/*.py[cod]
gmaps-output

# Proxy lists (may contain credentials) — keep local, never commit
restaurants/proxies-comma.txt
*proxies*.txt

# Scrape outputs — keep local, never commit (preserve the dir via .gitkeep)
restaurants/out/
!restaurants/out/.gitkeep

# Build artifacts — binaries don't belong in git
*.exe
