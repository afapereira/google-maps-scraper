diff --git a/adapters/fetchers/jshttp/jshttp.go b/adapters/fetchers/jshttp/jshttp.go
index cb1c2a9..d50ff66 100644
--- a/adapters/fetchers/jshttp/jshttp.go
+++ b/adapters/fetchers/jshttp/jshttp.go
@@ -3,6 +3,7 @@ package jshttp
 import (
 	"context"
 	"errors"
+	"runtime"
 
 	"github.com/playwright-community/playwright-go"
 
@@ -99,6 +100,7 @@ func New(params JSFetcherOptions) (scrapemate.HTTPFetcher, error) {
 		disableImages: params.DisableImages,
 		proxyPool:     pool,
 		ua:            params.UserAgent,
+		reuseLimit:    params.BrowserReuseLimit,
 	}
 	ans.factory = sessionFactory
 
@@ -202,6 +204,15 @@ func (o *jsFetch) Fetch(ctx context.Context, job scrapemate.IJob) scrapemate.Res
 
 	resp := job.BrowserActions(ctx, wrappedPage)
 
+	// On failure, swap the browser before the slot is reused: the next job
+	// (including this job's own retry) then runs on a fresh browser with the
+	// next proxy from the pool instead of a possibly banned/wedged one.
+	if resp.Error != nil {
+		_ = slot.releaseAfterFailure(ctx)
+
+		return resp
+	}
+
 	if cleanErr := slot.release(ctx); cleanErr != nil && resp.Error == nil {
 		resp.Error = cleanErr
 	}
@@ -255,32 +266,37 @@ func (o *browser) Close() {
 }
 
 func newBrowser(pw *playwright.Playwright, headless, disableImages bool, proxyPool *ProxyPool, ua string) (*browser, error) {
+	launchArgs := []string{
+		`--start-maximized`,
+		`--no-default-browser-check`,
+		`--disable-dev-shm-usage`,
+		`--no-sandbox`,
+		`--disable-setuid-sandbox`,
+		`--disable-gpu`,
+		`--mute-audio`,
+		`--disable-extensions`,
+		`--disable-breakpad`,
+		`--disable-features=TranslateUI,BlinkGenPropertyTrees`,
+		`--disable-ipc-flooding-protection`,
+		`--enable-features=NetworkService,NetworkServiceInProcess`,
+		"--enable-features=NetworkService",
+		`--disable-default-apps`,
+		`--disable-notifications`,
+		`--disable-webgl`,
+		`--disable-blink-features=AutomationControlled`,
+		"--ignore-certificate-errors",
+		"--ignore-certificate-errors-spki-list",
+		"--disable-web-security",
+	}
+	// --no-zygote/--single-process crash Chromium on Windows ("closed network
+	// connection" over CDP); only safe on Linux/containerized environments.
+	if runtime.GOOS != "windows" {
+		launchArgs = append(launchArgs, `--no-zygote`, `--single-process`)
+	}
+
 	opts := playwright.BrowserTypeLaunchOptions{
 		Headless: playwright.Bool(headless),
-		Args: []string{
-			`--start-maximized`,
-			`--no-default-browser-check`,
-			`--disable-dev-shm-usage`,
-			`--no-sandbox`,
-			`--disable-setuid-sandbox`,
-			`--no-zygote`,
-			`--disable-gpu`,
-			`--mute-audio`,
-			`--disable-extensions`,
-			`--single-process`,
-			`--disable-breakpad`,
-			`--disable-features=TranslateUI,BlinkGenPropertyTrees`,
-			`--disable-ipc-flooding-protection`,
-			`--enable-features=NetworkService,NetworkServiceInProcess`,
-			"--enable-features=NetworkService",
-			`--disable-default-apps`,
-			`--disable-notifications`,
-			`--disable-webgl`,
-			`--disable-blink-features=AutomationControlled`,
-			"--ignore-certificate-errors",
-			"--ignore-certificate-errors-spki-list",
-			"--disable-web-security",
-		},
+		Args:     launchArgs,
 	}
 	if disableImages {
 		opts.Args = append(opts.Args, `--blink-settings=imagesEnabled=false`)
diff --git a/adapters/fetchers/jshttp/session_slot.go b/adapters/fetchers/jshttp/session_slot.go
index 32f126c..0a7864d 100644
--- a/adapters/fetchers/jshttp/session_slot.go
+++ b/adapters/fetchers/jshttp/session_slot.go
@@ -22,6 +22,7 @@ type slotRuntime interface {
 	recreateContext() error
 	recreateBrowser() error
 	recycleIfNeeded() error
+	forceRecycle() error
 }
 
 type runtimeFactory interface {
@@ -95,12 +96,23 @@ func (s *sessionSlot) release(ctx context.Context) error {
 	return s.runtime.recycleIfNeeded()
 }
 
+// releaseAfterFailure swaps the browser out immediately so the next job on
+// this slot does not inherit a possibly banned proxy or wedged browser state.
+func (s *sessionSlot) releaseAfterFailure(_ context.Context) error {
+	if s.runtime == nil {
+		return nil
+	}
+
+	return s.runtime.forceRecycle()
+}
+
 type playwrightRuntimeFactory struct {
 	pw            *playwright.Playwright
 	headless      bool
 	disableImages bool
 	proxyPool     *ProxyPool
 	ua            string
+	reuseLimit    int
 }
 
 func (f *playwrightRuntimeFactory) create(context.Context) (slotRuntime, error) {
@@ -116,6 +128,7 @@ func (f *playwrightRuntimeFactory) create(context.Context) (slotRuntime, error)
 		disableImages: f.disableImages,
 		proxyPool:     f.proxyPool,
 		ua:            f.ua,
+		reuseLimit:    f.reuseLimit,
 	}, nil
 }
 
@@ -126,6 +139,8 @@ type playwrightRuntime struct {
 	disableImages bool
 	proxyPool     *ProxyPool
 	ua            string
+	reuseLimit    int
+	usage         int
 }
 
 func (r *playwrightRuntime) pageCount() int {
@@ -204,8 +219,31 @@ func (r *playwrightRuntime) recreateBrowser() error {
 	return nil
 }
 
+// recycleIfNeeded counts completed jobs on this browser and replaces it once
+// the reuse limit is hit. Recreating the browser pulls the NEXT proxy from the
+// pool, so with proxies configured this is what rotates them over time.
 func (r *playwrightRuntime) recycleIfNeeded() error {
-	return nil
+	if r.reuseLimit <= 0 {
+		return nil
+	}
+
+	r.usage++
+	if r.usage < r.reuseLimit {
+		return nil
+	}
+
+	r.usage = 0
+
+	return r.recreateBrowser()
+}
+
+// forceRecycle replaces the browser immediately — used after a failed job so
+// the next job on this slot starts on a fresh browser with the next proxy from
+// the pool instead of retrying through a possibly banned one.
+func (r *playwrightRuntime) forceRecycle() error {
+	r.usage = 0
+
+	return r.recreateBrowser()
 }
 
 type playwrightPage struct {
diff --git a/adapters/fetchers/jshttp/session_slot_internal_test.go b/adapters/fetchers/jshttp/session_slot_internal_test.go
index eb1e71c..a9f58d8 100644
--- a/adapters/fetchers/jshttp/session_slot_internal_test.go
+++ b/adapters/fetchers/jshttp/session_slot_internal_test.go
@@ -165,6 +165,10 @@ func (f *fakeRuntime) recycleIfNeeded() error {
 	return nil
 }
 
+func (f *fakeRuntime) forceRecycle() error {
+	return f.recreatePage()
+}
+
 func TestSessionSlotReusesHealthyPrimaryPage(t *testing.T) {
 	t.Parallel()
 
diff --git a/scrapemate.go b/scrapemate.go
index 2756753..1aeb8da 100644
--- a/scrapemate.go
+++ b/scrapemate.go
@@ -266,8 +266,16 @@ func (s *ScrapeMate) Start() error {
 					"speed", fmt.Sprintf("%.2f jobs/min", perMinute),
 				)
 
-				if s.exitOnInactivity && time.Now().UTC().Sub(lastActivityAt) > s.exitOnInactivityDuration {
-					err := fmt.Errorf("%w: %s", ErrInactivityTimeout, lastActivityAt.Format(time.RFC3339))
+				// Before any job finishes, lastActivityAt is zero; Sub would be ~"ages
+				// ago" and would falsely trip inactivity. Use scrape start time until
+				// the first completion.
+				activityRef := lastActivityAt
+				if activityRef.IsZero() {
+					activityRef = startTime
+				}
+
+				if s.exitOnInactivity && time.Now().UTC().Sub(activityRef) > s.exitOnInactivityDuration {
+					err := fmt.Errorf("%w: %s", ErrInactivityTimeout, activityRef.Format(time.RFC3339))
 
 					s.log.Info("exiting because of inactivity", "error", err)
 					s.cancelFn(err)
